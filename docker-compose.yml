services:
  postgres:
    image: postgres:15-alpine
    environment:
      POSTGRES_USER: a2amp
      POSTGRES_PASSWORD: a2amp_dev
      POSTGRES_DB: a2amp
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
